[project]
name = "agency"
description = "Automated multi-step workflows for AI coding agents."
version = "0.1.0"
requires-python = ">=3.14"

[dependency-groups]
dev = [
    "prek>=0.3.8",
    "pytest>=9.0.2",
    "ruff>=0.15.9",
    "ty>=0.0.28",
]
